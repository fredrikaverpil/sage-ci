# Generated by sage-ci - DO NOT EDIT

name: sage-ci-sync

on:
  schedule:
    - cron: '0 0 1 * *' # First day of each month
  workflow_dispatch:

jobs:
  sync:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-go@v5
        with:
          go-version: stable

      - name: Update sage-ci dependency
        run: |
          if [ -d ".sage" ]; then
            cd .sage && go get -u github.com/fredrikaverpil/sage-ci@latest && go mod tidy
          fi

      - name: Sync workflows
        run: |
          if [ -d "cmd/sage-ci" ]; then
            # run in sage-ci repo
            go run ./cmd/sage-ci sync
          else
            # run in any other repo
            go run github.com/fredrikaverpil/sage-ci/cmd/sage-ci@latest sync
          fi

      - name: Regenerate Makefile
        run: go run ./.sage

      - name: Create Pull Request
        uses: peter-evans/create-pull-request@v7
        with:
          commit-message: 'chore: sync sage-ci'
          title: 'chore: sync sage-ci'
          body: |
            Automated update from sage-ci.

            This PR updates:
            - `.sage/go.mod` (sage-ci dependency)
            - `.github/workflows/`
            - `Makefile`
          branch: chore/sage-ci-sync
          delete-branch: true
