# Generated by {{ .GeneratedBy }} - DO NOT EDIT

name: lua

on:
  push:
    branches: [main]
  pull_request:

{{- if .HasLua }}
jobs:
  format:
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        module: {{ toJSON .LuaModules }}
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-go@v5
        with:
          go-version: stable
          cache: false
      - name: format-check
        run: make run target=lua-format
        working-directory: ${{ "{{" }} matrix.module {{ "}}" }}
{{- end }}
