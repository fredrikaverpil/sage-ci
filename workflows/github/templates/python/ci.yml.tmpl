# Generated by {{ .GeneratedBy }} - DO NOT EDIT

name: python

on:
  push:
    branches: [main]
  pull_request:

jobs:
{{- if not .SkipPythonLint }}
  lint:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-go@v5
        with:
          go-version: stable
          cache: false
      - uses: astral-sh/setup-uv@v5
      - name: lint
        run: make python-lint
{{- end }}

{{- if not .SkipPythonFormat }}
  format:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-go@v5
        with:
          go-version: stable
          cache: false
      - uses: astral-sh/setup-uv@v5
      - name: format-check
        run: make python-format
{{- end }}

{{- if not .SkipPythonMypy }}
  type:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-go@v5
        with:
          go-version: stable
          cache: false
      - uses: astral-sh/setup-uv@v5
      - name: mypy
        run: make python-mypy
{{- end }}

{{- if not .SkipPythonTest }}
  test:
    strategy:
      fail-fast: false
      matrix:
        os: {{ toJSON .OSVersions }}
        python: {{ toJSON .PythonVersions }}
    runs-on: ${{ "{{" }} matrix.os {{ "}}" }}
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-go@v5
        with:
          go-version: stable
          cache: false
      - uses: astral-sh/setup-uv@v5
        with:
          python-version: ${{ "{{" }} matrix.python {{ "}}" }}
      - name: pytest
        run: make python-test
{{- end }}
