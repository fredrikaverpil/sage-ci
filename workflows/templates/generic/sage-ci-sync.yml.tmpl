# Generated by {{ .GeneratedBy }} - DO NOT EDIT

name: sage-ci-sync

on:
  schedule:
    - cron: '0 0 1 * *' # First day of each month
  workflow_dispatch:

jobs:
  sync:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-go@v5
        with:
          go-version: stable

      - name: Run sage-ci sync
        run: go run github.com/fredrikaverpil/sage-ci/cmd/sage-ci@latest sync

      - name: Create Pull Request
        uses: peter-evans/create-pull-request@v7
        with:
          commit-message: 'chore: sync sage-ci workflows'
          title: 'chore: sync sage-ci workflows'
          body: |
            Automated update from sage-ci sync.

            This PR updates:
            - `.sage/synced.go`
            - `.github/workflows/`
          branch: chore/sage-ci-sync
          delete-branch: true
