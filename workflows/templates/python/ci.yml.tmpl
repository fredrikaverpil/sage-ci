# Generated by {{ .GeneratedBy }} - DO NOT EDIT
# To update, run: make sync-workflows

name: python

on:
  push:
    branches: [main]
  pull_request:

{{- if .HasPython }}
jobs:
  lint:
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        module: {{ toJSON .PythonModules }}
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-go@v5
        with:
          go-version: stable
          cache: false
      - uses: astral-sh/setup-uv@v5
      - name: lint
        run: make python-lint
        working-directory: ${{ "{{" }} matrix.module {{ "}}" }}

  format:
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        module: {{ toJSON .PythonModules }}
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-go@v5
        with:
          go-version: stable
          cache: false
      - uses: astral-sh/setup-uv@v5
      - name: format-check
        run: make python-format
        working-directory: ${{ "{{" }} matrix.module {{ "}}" }}

  type:
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        module: {{ toJSON .PythonModules }}
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-go@v5
        with:
          go-version: stable
          cache: false
      - uses: astral-sh/setup-uv@v5
      - name: mypy
        run: make python-mypy
        working-directory: ${{ "{{" }} matrix.module {{ "}}" }}

  test:
    strategy:
      fail-fast: false
      matrix:
        module: {{ toJSON .PythonModules }}
        os: {{ toJSON .OSVersions }}
        python: {{ toJSON .PythonVersions }}
    runs-on: ${{ "{{" }} matrix.os {{ "}}" }}
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-go@v5
        with:
          go-version: stable
          cache: false
      - uses: astral-sh/setup-uv@v5
        with:
          python-version: ${{ "{{" }} matrix.python {{ "}}" }}
      - name: pytest
        run: make python-test
        working-directory: ${{ "{{" }} matrix.module {{ "}}" }}
{{- end }}
